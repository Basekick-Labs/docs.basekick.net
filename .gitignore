# Dependencies
node_modules/
/node_modules
.pnp
.pnp.js

# Production
/out
/.next
/.source
/dist

# Generated files
.cache-loader
.cache/

# Testing
/coverage
*.lcov

# Environment variables - NEVER commit these
.env
.env.*
!.env.example
*.local

# Secrets and credentials
*.key
*.pem
*.p12
*.pfx
*.cer
*.crt
*-key.json
credentials.json
secrets.json
config.local.*

# API keys and tokens
*apikey*
*api-key*
*token*
*secret*

# ...but documentation *about* tokens is not a credential. Without this the
# page is silently dropped from any clean checkout, so CI builds a site that
# is missing a page the author sees locally.
!docs-launchpad/using-the-console/tokens.md
!docs/**/*token*.md
!docs-arc-enterprise/**/*token*.md
!docs-launchpad/**/*token*.md

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.log

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Temporary files
*.tmp
*.temp
.temp/
.tmp/

# Build artifacts
*.tgz
*.zip
*.tar.gz

# Backup files
*.bak
*.backup
*~

# Claude Code: local machine state stays out, shared project docs go in.
# CLAUDE.md and the migration reference files describe how this repo is
# worked on - they are only useful if everyone (and CI) can read them.
.claude/*
!.claude/CLAUDE.md
!.claude/*.md
